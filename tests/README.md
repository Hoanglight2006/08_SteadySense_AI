# Kiểm thử

Chưa có mã nguồn nên chưa có test. Khi có project Android, đặt unit test theo
đúng quy ước Gradle (`src/app/src/test/...`) để `gradlew :app:testDebugUnitTest`
tự phát hiện — không đặt file test ở thư mục này, tương tự quy ước của
`07_FaceAccess_AI`.
