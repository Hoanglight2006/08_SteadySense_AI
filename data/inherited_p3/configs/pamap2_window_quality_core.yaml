run_name: pamap2_window_quality_core
seed: 42
device: cpu
data_dir: data/processed/pamap2_window_quality
output_dir: outputs
models:
  - fixed_fusion
  - attention_fusion
  - quality_fusion
  - proxy_quality_fusion
model:
  hidden_dim: 48
  quality_power: 0.5
training:
  batch_size: 512
  epochs: 8
  learning_rate: 0.001
  quality_loss_weight: 0.5
  num_workers: 0
abstention:
  confidence_threshold: 0.35
  mean_quality_threshold: 0.35
