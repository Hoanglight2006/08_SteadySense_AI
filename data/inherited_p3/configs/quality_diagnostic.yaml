run_name: quality_diagnostic_seed42
seed: 42
device: cpu
data_dir: data/processed/quality_diagnostic
output_dir: outputs
synthetic:
  modalities: 2
  embedding_dim: 48
  classes: 4
  samples_train: 1200
  samples_val: 300
  samples_test: 400
  missing_probability: 0.05
  quality_beta_alpha: 1.1
  quality_beta_beta: 1.1
  base_noise: 0.05
  quality_noise_scale: 3.0
  quality_noise_power: 1.0
model:
  hidden_dim: 64
training:
  batch_size: 128
  epochs: 12
  learning_rate: 0.001
  quality_loss_weight: 0.8
  num_workers: 0
abstention:
  confidence_threshold: 0.45
  mean_quality_threshold: 0.35
