# Preregistration nội bộ — SteadySense pilot v1

Chỉ điền và khóa sau smoke pilot 3 người, trước khi thu phần dataset còn lại.
Mọi thay đổi sau khóa phải có changelog; không dùng test để chỉnh ngưỡng.

- Protocol/data schema/app version + hash:
- Participant target và tiêu chí loại:
- Condition/session target:
- Split seed và tỷ lệ theo participant:
- Window/sample-rate/clock tolerance:
- Rule-quality thresholds:
- Completion rule và abstention rule:
- Primary metrics: macro-F1, false completion, false rejection, risk–coverage:
- Secondary metrics: cycle MAE, ECE, latency/RAM/pin:
- Giới hạn false rejection cho phép khi so model:
- Bootstrap CI theo participant:
- Model selection bằng validation; thời điểm/người mở test:
- Phân tích ablation đã định trước:
- Câu được/không được tuyên bố:

